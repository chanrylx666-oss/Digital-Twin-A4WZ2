# 代码审查

`Area3D` 重叠优先，中心距离阈值作为受限兜底。四个配置转子必须都在夹取范围内，才会一起挂接并进入后续旋转；任意一个不满足时整次循环复位，避免部分抓取。

代码整理采用 Godot C# 的 `partial` 组件组织：公共状态首先移到独立文件，后续职责可无缝继续从主组件移出而不会影响 Inspector 绑定。

重构完成：入口、运动、工件搬运与测速模块分别位于独立的 partial 文件；所有导出字段仍属于同一个 Godot `TransferArm` 组件。
