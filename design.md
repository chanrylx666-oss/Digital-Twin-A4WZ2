# 转子搬运与上料回转设计

## 目标

在现有搬运节拍中，转臂的四组夹具会同时接管转子、转子2、转子3、转子4。夹取时四个转子重挂为同一机械手挂点的子节点并保持世界变换；放件完成时同时重挂到目标工位。上料回转在上料台转子被接管后并行转动。

## 检测策略

优先使用场景中配置的 `Area3D` 重叠检测；若模型暂未设置物理碰撞体，则使用可配置的中心点距离阈值作为临时几何检测，避免错误夹取。两种方式都由 Inspector 的节点引用配置，不依赖中文节点名称。

## 范围

只扩展 `TransferArm` 和主场景配置，不改变现有升降、夹爪、旋转、测速时序。
